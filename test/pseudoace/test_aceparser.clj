(ns pseudoace.test-aceparser
  (:require [pseudoace.aceparser :as sut]
            [clojure.test :as t]))

;; TODO: use cognitect.transcriptor
;;      - record REPL session
;;      - replay + fixup as tests
(deftest test-parse
  (t/testing "Deletes"
    (assert false))
  (t/testing "Renames"
    (assert false))
  (t/testing "New data"
    (assert false)))

(deftest test-parse-patch
  (t/testing "Deletes"
    (assert false))
  (t/testing "Renames"
    (assert false))
  (t/testing "New data"
    (assert false)))
